# Qdrant Workshop Dependencies - Shareable Edition
# This file ensures the workshop can run independently

# Core dependencies (required for all notebooks)
qdrant-client>=1.8.0
pandas>=2.0
numpy>=1.24
tqdm>=4.66
matplotlib>=3.8
scikit-learn>=1.3

# JupyterLab and notebook support
jupyterlab>=4.0
ipywidgets>=8.0
notebook>=7.0

# Embedding and ML dependencies
fastembed>=0.3

# LLM APIs (optional - for Notebook 5: Agentic RAG)
openai>=1.0
anthropic>=0.18

# Utility packages
python-dotenv>=1.0  # For .env file support
requests>=2.31  # For HTTP operations

# Development and testing (optional)
pytest>=7.0
black>=23.0
flake8>=6.0

# Note: Install with: pip install -r requirements.txt
# For minimal install (core only): pip install qdrant-client pandas numpy tqdm matplotlib scikit-learn jupyterlab